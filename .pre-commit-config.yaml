repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.6
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: local
    hooks:
      - id: biome-check-v2
        name: biome check (frontend-v2)
        entry: bash -c 'cd frontend-v2 && npx biome check --fix'
        language: system
        files: ^frontend-v2/.*\.(ts|tsx|js|json)$
        pass_filenames: false
      - id: expo-lint-mobile-v3
        name: expo lint (mobile-v3)
        entry: bash -c 'cd mobile-v3 && npx expo lint 2>&1 | head -30; exit ${PIPESTATUS[0]}'
        language: system
        files: ^mobile-v3/.*\.(ts|tsx)$
        pass_filenames: false
